Used for empty History and first-run Capture.

```jsx
<EmptyState icon="list" title="Nothing captured yet" body="Everything you speak will land here." />
```

Never apologetic, never an illustration. The hairline ring around the glyph deliberately echoes the capture well.

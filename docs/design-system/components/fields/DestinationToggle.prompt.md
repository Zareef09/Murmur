Switches a capture between Reminders and Calendar. Used on the Confirmation sheet and in Settings as the default-destination preference.

```jsx
<DestinationToggle value={dest} onChange={setDest} />
```

Two options, never three. The selected option carries icon, label, tint and a hairline — color is never the only difference.

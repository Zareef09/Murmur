Circular icon-only target for nav, dismissal and secondary entries (History, Settings).

```jsx
<IconButton name="list" label="History" />
<IconButton name="x" label="Close" tone="surface" />
```

Keep `size` at 44 or above. `accent` tone is reserved — on the capture screen the accent already belongs to the capture element.

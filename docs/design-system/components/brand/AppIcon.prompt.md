The Home Screen icon — the capture well at its smallest legible size.

```jsx
<AppIcon size={120} />
<AppIcon size={60} theme="dark" />
```

Two rings only (three collapse into mush below 60px), core at 38% inset, glow scaled to size. Deliberately not a microphone: the mark reads as a light left on, i.e. something being kept for you.
